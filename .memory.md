# n2n_bm 备忘

本目录是给 macOS（Apple Silicon）用的 n2n 3.1.1 源码副本 + 一些运行脚本。
以下内容是这台机器上做过改动的记录，换机器/升级上游前先读一遍。

## 为什么要改（背景）

- 上游 n2n 在 macOS 上走 **tuntaposx 内核扩展**（`/dev/tap0..254`）。该 kext 是 x86 时代的产物，
  Apple Silicon 上没有 arm64 版本、新版 macOS 也不再允许加载，所以预编译的 x86 edge 在这台机器上
  「unable to open any tap devices」必然失败（不是配置问题，sudo 也救不了）。
- macOS 自带的可用替代是 **utun**（内核内置，无需 kext，但需要 root）。utun 是**三层**设备（只收发 IP 包），
  而 n2n edge 的内部逻辑期望**二层** tap（以太帧）——多播判定、ARP、MAC 寻址都依赖以太头。
- 因此改法是：**在 tuntap 层加 utun 后端 + 用户态模拟二层**，edge 其余代码基本不动。

## 改了什么（上游 3.1.1 之上的 diff，共 3 个文件）

| 文件 | 改动 |
| --- | --- |
| `src/tuntap_osx.c` | 新增 utun 后端（约 550 行）：建 utun、配接口/路由、以太帧封装解封装、ARP 缓存与应答；`/dev/tap` 路径保留为回退 |
| `include/n2n.h` | 新增 `n2n_eth_tx_hook`（函数指针）声明，供设备层把自造帧交回 edge |
| `src/edge_utils.c` | 定义并实现该 hook，在 `edge_init()` 里注册（`edge_send_packet2net` 是 static 上下文，必须挂在 edge 侧） |

工具链副产物（`configure`、`config.mak`、`*.o`、`*.d`）都是 `autogen.sh + configure + make` 生成的，不在 diff 里。

### 后续修复（提交 `22a0d27`，代码审查后补）

- **pend 队列即时补发**：新增 `utun_pend_flush()`。一旦知道邻居 MAC 就立刻把暂存包补上以太头、
  经 `n2n_eth_tx_hook` 交给 edge；`tuntap_write()` 的 ARP 分支与 IP 帧分支各挂了一处调用。
  在此之前只能等 `tuntap_read()`——它由 `select()` 的 `FD_ISSET(device.fd)` 驱动，应用在等回包时不会
  再发包，导致首个包要等重传或 5s 超时被丢。
- **pend 遍历次序**：`tuntap_read()` 内改为"先清超时 → 再发所有已可解析的 → 最后才重试 ARP"，
  避免某个待重试条目挡住后面本来就能发的包。
- **hook 生命周期**：`edge_term()` 里把 `n2n_eth_tx_hook` 与静态 eee 指针置 `NULL`，避免悬垂。
- **ifconfig / route 失败可见**：`system()` 返回非零时打 `TRACE_WARNING`，**但不判失败**——
  路由已存在时 `route -n add` 同样返回非零，判失败会把正常重启搞挂。
- **验证方法**：重启服务清空 ARP 缓存后，用 `ping -c 1`（单包，不给重传机会）逐个打社区内对端，
  首包即通、0% 丢包，说明即时补发链路有效。

## 运行时机制（排错时看这里）

```
内核 ↔ utunN(fd) ↔ tuntap_osx.c 的 L2 垫片 ↔ n2n edge ↔ UDP(supernode/peer) ↔ 社区里其它 tap 节点
```

- **出方向**（内核 → utun → 社区）：读到的 IP 包前面补 14 字节以太头。
  目的 MAC 来源：广播/多播地址直接映射（`ff:ff:..`、`224/4 → 01:00:5e:xx`），单播查 ARP 缓存。
- **ARP 解析**（缓存里没有目标 IP 时）：把 IP 包暂存到 pending 队列，并从 `tuntap_read()` **返回一个 ARP
  请求帧**——因为 `tuntap_read` 的返回值正是 edge 拿去发往社区的「从 tap 读到的包」，所以不需要额外接口。
  对端应答后写入缓存，并**立即**把暂存的 IP 包补上以太头、经 `n2n_eth_tx_hook`（`utun_pend_flush()`）交给
  edge 发出。不能只依赖 `tuntap_read()`：它仅在 utun 有新包可读时才被调用，而应用在等回包时不会再发包，
  否则首个包要等重传或 5s 超时被丢。`tuntap_read()` 里保留了同样的排空逻辑作为兜底。
  参数：ARP 重试 1s、pending 超时 5s（丢弃）、ARP 条目老化 600s、缓存 64 条、pending 队列 8 个。
- **ARP 应答**（对端问我们的 IP）：发生在 `tuntap_write()` 内部，**无法**通过返回值送出，
  所以走 `n2n_eth_tx_hook` 直接交给 edge 广播/单播。hook 是**单实例全局**（`edge_init` 里赋值），
  一个进程只能跑一个 edge，嵌入多 edge 的场景需要改成按实例回调。
- **入方向**（社区 → 社区 → 网卡）：剥掉以太头，前面加 4 字节地址族前缀（`00 00 00 02`）写入 utun。
- **接口配置**：`ifconfig utunN inet <ip> <网络地址> netmask <mask> mtu <mtu> up`
  （utun 是点对点接口，必须给对端地址，这里用网段网络号，它不可能是主机地址），
  再加 `route -n add -net <net> -netmask <mask> -interface utunN` 把整个网段指过来。
- **模拟 MAC**：取第一个非 lo 的 AF_LINK 接口 MAC，置本地管理位、清多播位（`mac[0] = (mac[0] & 0xFC) | 0x02`）。
  与真网卡 MAC 不同、同机稳定，可用 `-m` 覆盖。
- **设备名不固定**：`utun4`、`utun5`… 由内核挑空闲 unit，启动日志里会打印实际名字。

## 已知限制 / 可能的坑

1. **只支持 IPv4**。IPv6/NDP 没实现，遇到 IPv6 包会丢弃并每 60s 打一条 warning。
   如果社区里有人只用 IPv6，这台机器看不到它。
2. **必须 root**（建 utun 需要）。launchd 里是 root 跑，跑完会 `setuid(nobody)`（日志里的 `uid=-2`，正常）。
3. **同一 IP/MAC 只能有一个实例在社区里注册**。旧的挤不掉新的：新实例会收到
   `REGISTER_SUPER_NAK: MAC or IP address already in use`。supernode 侧有 TTL，
   等一会儿或切到另一个 supernode 即可恢复。重启服务太快时偶尔会看到这条，不是坏了。
4. **`intoa()` 是倒着填 buffer 的**（从 `buf[len-1]` 往前写），必须用它的**返回值**，
   直接传 buffer 会得到垃圾字符串（踩过：`ifconfig: \006: bad value`）。新代码别再犯。
5. **跨网段不通**：自动加的路由只覆盖 `-a` 里那个掩码的范围（就是 conf 里 `-a` 的网段）。
   对端在别的网段要手工 `route add -host <ip> -interface utunN`。
6. **延迟高不一定是 utun 的问题**：经 supernode 中继（日志里 `pSp`）就是几百 ms，
   直连（`p2p`）才低。看 `python3 scripts/n2n-ctl edges` 的 mod 列。
7. **日志缓冲**：`traceEvent` 写 stdout，重定向到文件时是全缓冲——进程被 `kill -9` 会丢掉整段日志
   （排查时以为没启动，其实是没 flush）。调试时用 `script -q /tmp/x.log ./edge ...`，或注意这点。
   以 daemon 方式运行时 n2n 会把日志转去 syslog（本仓库的 plist 用 `-f` 前台跑 + 重定向到文件，不受影响）。
8. **不要跑第二个实例**调试：端口 50001、管理口 5644、社区注册都会冲突（见第 3 条）。
9. **删除/重开设备**：`tuntap_close()` 会清掉 utun 状态；fd 关闭时接口自动消失（路由也随之消失），不需要手工清理。
10. **`-d <设备名>` 在 3.x 不存在**（v2 时代的选项，日志里会打 `WARNING: unknown option -d` 后退出）。
    conf 里那行已注释掉。
11. **上游升级**：tuntap_osx.c 是唯一的大改点，`n2n.h` 只多了 hook 声明、`edge_utils.c` 只有 hook 注册，
    rebase 时先看这三处；`n2n_eth_tx_hook` 千万别丢，少了它 ARP 应答发不出去（对端一直 ARP 但连不上）。

## 使用方式的变化（相对原版）

- 不再需要装/启用 tuntaposx kext；不再需要 `-d`；接口名是内核给的 `utunN`，不能自定义。
- 启动必须 `sudo`（或 launchd 以 root 运行）。
- 其余参数含义不变：`-c` 社区、`-l` supernode、`-a ip/掩码`、`-p` 本地 UDP 端口、`-r` 允许转发、`-k` 加密密钥。
- `-a` 的**掩码现在决定自动加的路由**（多层社区要注意）。
- 目前 **没有设 `-k`**（null cipher），经 supernode 中继时是明文，且该社区里还有别人的节点。
  要开加密必须所有节点用同一个 key，否则会互相隔离。

## 编译 / 部署 / 日常操作

```bash
# 编译（产物在 n2n/：edge、supernode，原生 arm64）
cd n2n && ./autogen.sh && ./configure && make -j8

# 部署（当前装法：系统级 LaunchDaemon，开机自启）
sudo install -m 755 n2n/edge /usr/local/bin/edge
sudo install -m 644 conf/edge.conf /usr/local/etc/n2n/edge.conf
sudo install -m 644 lomot.n2n.edge.plist /Library/LaunchDaemons/
sudo launchctl bootout system /Library/LaunchDaemons/lomot.n2n.edge.plist 2>/dev/null
sudo launchctl bootstrap system /Library/LaunchDaemons/lomot.n2n.edge.plist

# 改完源码/配置后
sudo cp n2n/edge /usr/local/bin/edge && sudo launchctl kickstart -k system/lomot.n2n.edge
```

常用检查：

```bash
launchctl print system/lomot.n2n.edge | head -5     # 服务状态（launchctl list 看不到系统域）
tail -f /var/log/n2n-edge.log                        # 日志（plist 里配的 StandardOutPath）
ifconfig utun4; netstat -rn -f inet | grep <你的网段>
python3 n2n/scripts/n2n-ctl edges                    # 对端列表（管理口 UDP 5644）
ping <对端 overlay IP>
```

排错技巧：`./edge conf/edge.conf -v -v` 会打印 `Tx PACKET ... 42 bytes`（ARP）和 `Rx TAP packet (42)`，
可以用来确认「ARP 请求有没有发出去 / 对端 ARP 有没有收进来」这条链路。

## 本机环境备注

- 机器是 arm64 + 较新的 macOS；`n2n/` 下的产物是原生 arm64（之前的根目录二进制是 x86，靠 Rosetta 跑，已替换）。
- `autoconf/automake` 来自 Homebrew（`/opt/homebrew/bin`），`configure` 里的 openssl 头文件也是 brew 的。
- **外接卷 `/Volumes/SA500` 对「osascript 提权的 root shell」不可读**（macOS TCC 限制），
  用 `osascript ... with administrator privileges` 装包/跑脚本时要先把文件 `cp` 到 `/tmp` 再操作，
  否则会报 `Operation not permitted` / `config file not found`。
- 实测环境：公共 supernode 地址、社区名、本机 overlay 地址都在 `conf/edge.conf` 里
  （**此处不写，避免随公开仓库泄露**）。社区内多台对端 `ping` 0% 丢包。
